# performance

## first moment

* 对于需要交换空间顺序可以优化的想法，已经实现。
* 无法把样本平均分到每一个比特上面，这是因为样本平均省略了概率。

## purity

* 对于纯度；凡是可以用 dense matrix 都应该用 dense; 对于MPO格式本身就无法优化。
* 纯度的复杂度根本上来自于系统变大了 $tr[\rho \otimes \rho SWAP]$ , 但是又没有真的完全纠缠，所以复杂度是多项式级别提升，非常合理。
* 纯度的并行计算是否有效取决于带宽是否是性能瓶颈。
